# Handoff — Resume from 2026-04-23 (Thu)

Short resumen de lo shipeado hoy + pendientes al cierre. Para contexto anterior ver `docs/HANDOFF-2026-04-21.md`.

---

## Shipped hoy (en prod)

### PR [#13](https://github.com/fricox/smoothfenceusa/pull/13) — Hearth widget en LP oculta
Squash-mergeado a `main` como commit `8ef6d05`, Vercel deploy SUCCESS.

**Qué hace:**
- Agrega ruta oculta `/financing/calculator` con el widget oficial de Hearth (script `widget.gethearth.com/script.js` + iframe `#hearth-widget_calculator_v1`, `data-orgid="61334"`, `data-partner="smooth-fence-usa"`).
- Público `/financing` **no** cambió — sigue con el diseño estático (CheckIcon + benefits panel) porque la integración visual del iframe no funcionó.

**Hiding discipline aplicada:**
- Page metadata: `robots: { index: false, follow: false }`.
- `app/robots.ts`: `/financing/calculator/` agregado al disallow (junto a `/api/`, `/pay/`, `/pay/success/`).
- Sin link en nav, footer, ni `app/sitemap.ts`.

**Files touched:**
- `app/financing/calculator/page.tsx` (new) — server component, metadata.
- `app/financing/calculator/HearthWidgetEmbed.tsx` (new) — client component, `<Script>` + iframe.
- `app/robots.ts` — disallow entry.
- `components/sections/HearthPaymentCalculator.tsx` — sigue en su versión estática original (un attempt de integrar el widget acá se revirtió).

**Checks post-deploy ejecutados:**
- `/financing/calculator` → HTTP 200, `<meta name="robots" content="noindex, nofollow">` ✓
- `/robots.txt` → `Disallow: /financing/calculator/` ✓
- `/financing` → sin iframe (página estática intacta) ✓
- `/sitemap.xml` → sin referencia a `/financing/calculator` ✓

**Nota sobre `data-orgid` / `data-partner`:** no aparecen en el HTML SSR porque Next.js `<Script strategy="afterInteractive">` inserta el tag client-side. Los atributos sí están presentes en el DOM una vez que hidrata — hay que abrir la URL en Chrome/Safari para confirmar que el script de Hearth corre con la atribución correcta.

---

## Pendientes al cierre

### P0 — Validación manual del widget (requiere Fede + navegador real)
- [ ] Abrir https://smoothfenceusa.com/financing/calculator en Chrome.
- [ ] Devtools → Network: confirmar que `widget.gethearth.com/script.js` carga HTTP 200.
- [ ] Devtools → Elements: confirmar que el `<script>` inyectado tiene `data-orgid="61334"` y `data-partner="smooth-fence-usa"`.
- [ ] Visual: el iframe `#hearth-widget_calculator_v1` se popula con la calculadora live (no queda en blanco).
- [ ] Console: sin errores de CORS / X-Frame-Options / partner token.
- [ ] Mobile: misma URL en iPhone real, verificar responsividad y tappability.

### P0 — WIP local sin commitear en `feat/p0-5-fency-kb-refactor`
`git status` en esa rama muestra 6 archivos modificados + 4 docs untracked. Ninguno relacionado al widget (el widget ya está en prod).

**Modified (hardening del 2026-04-20):**
- `app/api/checkout/route.ts` — reemplaza `payment_method_types: ["card"]` por `automatic_payment_methods: { enabled: true }`.
- `app/pay/page.tsx` — banner amarillo TEST MODE cuando `pk_test_*`.
- `lib/sheets.ts` — `pushLeadEvent` con 3 retries + fallback a `/tmp/leads-fallback.jsonl`.

**Modified (P0-5 Fency KB refactor — ya hay PR #12 mergeado pero estos cambios locales son posteriores):**
- `smoothfence-chatbot/lib/chatbot/core/engine.ts` — pasa `input.language` a `loadKnowledgeBase()`.
- `smoothfence-chatbot/lib/chatbot/knowledge/loader.ts` — loader de 3 tiers (site `/api/kb` → Supabase → `EMPTY_KB`), timeout 3s, revalidate 300s.

**Modified (previo al widget):**
- `components/sections/HearthPaymentCalculator.tsx` — este archivo local tiene la versión del widget embebido (de cuando intentamos integrarlo en `/financing` directamente). **Antes de commitear, revertir este archivo** a la versión de `main` (que ya fue mergeado en PR #13).

**Untracked (docs creados en sesiones anteriores):**
- `docs/HANDOFF-2026-04-21.md`, `docs/MEMORY.md`, `docs/go-live-checklist.md`, `docs/sandbox-test-report-2026-04-20.md` — todos agregados en commits históricos de docs.

**Pasos sugeridos para limpiar esta rama:**
1. `git checkout main -- components/sections/HearthPaymentCalculator.tsx` (descartar la versión local del widget; ya está en prod via LP oculta).
2. `git add app/ lib/ docs/` y commitear P0 Stripe hardening (mensaje: "pre-live hardening: auto payment methods, test banner, CRM retry + fallback").
3. `git add smoothfence-chatbot/` y commitear KB refactor (mensaje: "P0-5: three-tier KB loader with site endpoint + Supabase fallback").
4. `git push` y abrir PRs separados para Stripe hardening y KB refactor, o un solo PR bundled si preferís.

### P1 — Design decision para Hearth widget
Una vez validado el widget en prod, Fede tiene que decidir cómo lo integra en la experiencia pública. Opciones (expandidas en `memory/project_hearth_widget.md`):
- Rework del layout de `/financing` para que el iframe encaje.
- Mantener la LP oculta como "la calculadora real" y agregar en `/financing` un botón "Ver mi pago exacto →" que linkee ahí (rompería el hiding si se agrega a nav; fine si es solo inline).
- Descartar el widget y quedarse con el panel estático + CTA de apply.

### P1-P2 — Sandbox test improvements pendientes
Del `docs/sandbox-test-report-2026-04-20.md` quedan items 5-18 sin atacar. Los más valiosos post go-live (repetidos del handoff anterior):
- #5 Context/escape hatch en `/pay` cuando se llega sin prefill.
- #7 Delay auto-open del Fency widget en rutas de conversión (`/pay`, `/pay/success`).
- #8 Sacar PII del query string de `/pay`.
- #11 Enriquecer metadata de Stripe Session (source, utm, linearFeet).

### Go-live (cuando estés listo)
`docs/go-live-checklist.md` está listo para ejecutarse. Regla de oro: ventana de 30 min mínimo, con Fede presente, no-viernes.

---

## Heads-up de esta sesión

Durante los post-merge checks corrí por error `git checkout origin/main -- .` en el branch `feat/p0-5-fency-kb-refactor`, que clobbereó los 6 archivos WIP. Recuperé todo desde el stash dropped (SHA `e0bafc1`) via `git stash apply`. Verificado: `git diff --stat` matchea exactamente el preview del stash (155 insertions, 44 deletions, mismos archivos). Nada perdido, pero es un recordatorio de no usar `checkout <ref> -- .` con pathspec amplio cuando hay WIP dirty.

---

*Próxima pickup: validar el widget en Chrome + decidir si limpiás la rama actual en múltiples PRs o un bundle. Nos vemos la próxima.*
